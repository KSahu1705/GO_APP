// package queries

// const (
// 	queryInsertMigratedData = `
// 			INSERT INTO

// 				(product_id, d_id_seller, d_id_current, d_id_jarvis, status, update_by, update_time, product_name, score, event_type, version, shop_type)
// 			VALUES($1, $2, $3, $4, $7, 0, now(), $5, $6, $8, $9, $10)
// 		`
// )
